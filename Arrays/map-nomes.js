const nomes = ['Ana Julia', 'Caio Vinicius', 'Bia Silva'];

const nomesPadronizados = nomes.map((nome) => nome.toLocaleUpperCase());
// console.log(nomesPadronizados);

const nums = [1, 2, 3, 4];

const numsNovo = nums.map((numeros) => numeros * 10);
// console.log(numsNovo);